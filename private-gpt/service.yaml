apiVersion: v1
kind: Service
metadata:
  name: private-gpt
  namespace: private-gpt
spec:
  selector:
    appID: private-gpt
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
