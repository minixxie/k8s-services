apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: tools
spec:
  rules:
  - host: grafana.local
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: grafana
            port:
              number: 80
